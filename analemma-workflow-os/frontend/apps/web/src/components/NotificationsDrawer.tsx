import React from 'react';

// Deprecated: Notifications are now handled in `WorkflowMonitor`.
// This module remains for compatibility but renders nothing.
const NotificationsDrawer: React.FC = () => null;

export default NotificationsDrawer;
